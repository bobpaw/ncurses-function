language: cpp

git:
  depth: false

dist: xenial

os:
  - linux
  - osx

addons:
  apt:
    packages:
    - ncurses-dev
  homebrew:
    packages:
    - ncurses-dev

compiler:
  - gcc
  - clang

script:
  - cmake -S . -B build && cmake --build build
  - echo "q" | build/src/graph
